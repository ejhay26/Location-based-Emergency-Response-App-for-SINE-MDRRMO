APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://nuclei-oil-modular.ngrok-free.dev

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql

# DB_HOST=mdrrmo-sine-db-mddrmo-sine.k.aivencloud.com # host.containers.internal # 127.0.0.1 , containers for podman
# DB_PORT=24346
# DB_DATABASE=defaultdb
# DB_USERNAME=avnadmin # Make sure to replace with your actual database credentials (Don't use 'root' account)
# DB_PASSWORD= # Make sure to replace with your actual database credentials (Don't use 'root' account)
# DB_SSLMODE=REQUIRED

# For local development, use the following values instead of the Aiven ones above.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=emergencydb
DB_USERNAME=ejhay
DB_PASSWORD=ejhay

SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=s3 # 's3' talks to whichever S3-compatible endpoint AWS_ENDPOINT below points at (R2 in prod, MinIO locally). Set to 'public' only for pure local-disk storage with no S3-compatible service at all.
QUEUE_CONNECTION=database

CACHE_STORE=file
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_SCHEME=null
MAIL_HOST=smtp.gmail.com
MAIL_PORT=465
MAIL_USERNAME=ejcp2005@gmail.com
MAIL_PASSWORD=  # change this with the app passwordz
MAIL_FROM_ADDRESS="no-reply@sinemdrrmo.gov.ph"
MAIL_FROM_NAME="MDRRMO SAN ISIDRO NUEVA ECIJA EMERGENCY RESPONSE APP"

PHILSMS_API_TOKEN=
PHILSMS_SENDER_NAME="PhilSMS"
# dashboard.philsms.com

FIREBASE_PROJECT_ID=mdrrmo-sine-response-app
FIREBASE_CREDENTIALS=storage/app/mdrrmo-sine-response-app-firebase-adminsdk-fbsvc-73bd4e4846.json

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=auto
AWS_BUCKET=mdrrmo-sine-storage
AWS_USE_PATH_STYLE_ENDPOINT=true

VITE_APP_NAME="${APP_NAME}"
